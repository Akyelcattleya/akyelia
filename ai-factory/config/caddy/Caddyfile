# ============================================
# AI FACTORY - Caddy v2 Configuration
# ============================================

:80 {
    root * /srv
    file_server
    try_files {path} /smart-terminal.html

    # API Proxy vers Smart Router (sans couper le préfixe /api)
    handle /api/* {
        reverse_proxy smart-router:8765
    }
}
